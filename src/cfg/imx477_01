<camera_dev>
    <cam>
        <still>
            <val>rpicam-still -v 0 --camera 0 -t 0.5 -n -o</val>
        </still>
        <vid>
            <val>rpicam-vid -v 0 --camera 0 --bitrate 10mbps -t 8s -n -o</val>
        </vid>
    </cam>
    <event>
        <tim_to>
            <val>60000</val>
        </tim_to>
        <tim_rep>
            <val>60000</val>
        </tim_rep>
    </event>
</camera_dev>
